# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local

# Env
.env
.env.local

# Logs
logs
*.log
npm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Postgres data volume is Docker-managed, not filesystem-local — nothing to
# ignore here, but a reminder this repo assumes `docker compose up` for db.

# Worktree metadata (parallel-agent worktrees, docs on this workflow are in
# the session history, nothing here belongs in version control)
.claude/
